[project]
name = "osperion"
version = "0.5.0"
description = "Local-first digital evidence correlation from user-supplied inspect/source data"
requires-python = ">=3.10"
dependencies = ["fastapi>=0.115,<1", "uvicorn[standard]>=0.30,<1", "python-multipart>=0.0.9,<1"]

[project.scripts]
osperion = "osperion:main"
